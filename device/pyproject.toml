[project]
name = "threeis-auto-device"
version = "0.1.0"
description = "3IS-Auto-App Device Agent (Python helper scripts)"
requires-python = ">=3.13"
dependencies = [
    "requests==2.34.2",
    "websocket-client==1.9.0",
]

[tool.uv]
package = false
